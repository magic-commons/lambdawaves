# BEYOND THE FRONTIER · λWAVES — Round 3 · FABLE (lead)

*Reply to Round 2 (Opus, `02-opus.md`). Seats as before: KNOWN (author, year) · DERIVED-HERE · MEASURED · UNVERIFIED (with the test) · REFUTED (by what number). The corpus stays hidden from me this round by Josh's rule; I answer its items through Opus's citations only. Units atomic; $E_n=-1/(2n^2)$, $T_{\rm cl}=2\pi\bar n^3$, $T_{\rm rev}=\tfrac{4\pi}{3}\bar n^4$, $\beta_3=8\pi\sigma^3/(3\bar n)$, $\beta_4=10\pi\sigma^4/(3\bar n^2)$.*

**Evidence.** Probes `research/probes/bf-r3-airy.py` (the Airy closed form and the exact peak law), `bf-r3-probes.py` (branch tracking, the arithmetic table, the Runge–Lenz element by sympy), `bf-r3-poisson.py` (the ladder as a Poisson sum of Airy envelopes), `bf-r3-stokes.py` and `bf-r3-stokes2.py` (the exact series of $\alpha^*$: twelve terms by sympy, then sixty terms in rational arithmetic, and its large-order growth), `bf-r3-comb.py`, `bf-r3-comb2.py` and the inline run reported in §1.5 (comb packets), `bf-r3-fq.py` (the shell over $\mathbb F_q$), `bf-r3-far.py` (the exact far-field census), `bf-r3-components.py`, `bf-r3-morse.py`, `bf-r3-morse2.py` (the nodal set's topology), `bf-r3-recon5.py` (the blind search for generic reconnections), `bf-r3-recon6.py` (the census by the two-curve intersection, Theorem C5), `bf-r3-vortex3.py`. Several of these ran *before* Round 2 landed; where they and Opus reached the same number independently I say so, because two labs agreeing by different routes is the only kind of agreement this protocol trusts.

---

## 0 · Verdicts on Round 2

| # | Round-2 claim | Verdict | The number |
|---|---|---|---|
| A1a, coefficient 54 | "$\alpha^*=-3\beta+54\beta^3$" | **CONCEDED** — Round 1's $99/2$ is DEAD | I re-derived it: my stationarity equation dropped $-\tfrac{45}{2}\alpha\beta^3$ from $-\tfrac12\varepsilon^2$ and $3\beta\alpha^2$ from $\beta\alpha^3$; with both, $C=27-108+135=54$. And the exact series (§1.3) has $C_2=54$ as an integer. |
| §1.2 series through $\beta^7$; $\beta^9$ "MEASURED only, $\approx-1.6956\times10^8$" | | **CONFIRMED and CLOSED** | Exact: $C_5=-169\,746\,192$. Your measurement was right; both of your symbolic values were wrong. Twelve exact integer coefficients are in §1.3. |
| Theorem 1 (unique global maximiser) | | **STANDS**, independently | My branch tracking (`bf-r3-probes` A) found principal $=$ global at all 19 values $\beta\in[0.02,4]$, e.g. $\alpha^*=-1.6022965$ at $\beta=2$, $-2.1288014$ at $\beta=4$. |
| §1.4 floor $e^{-1/(54\beta^2)}$ | | **STANDS as the Borel singularity**; the exponent is new | $C_{k+1}/C_k\to-54k$: ratios $/(-54k)=0.978,1.021,1.031,1.030,1.026,1.022,1.017,1.014$ for $k=3\ldots10$. Stokes exponent and constant: §1.3, Theorem A6. |
| Ω₁ (mechanism) DEAD | | **CONCEDED**, and reached independently | `bf-r3-poisson` (run before your round): the *cubic* ladder is reproduced by the Poisson–Airy sum to $\le6\times10^{-3}$ with **no arithmetic**, e.g. $0.7414$ vs $0.7476$ at $(30,2)$; the exact ladder's excess over it, $0.8055-0.7414=0.064$, is $\beta_4$. Same kill, two roads. |
| Ω₁′ ("a real-analytic function of $(\beta_3,\mu)$ alone, up to Poisson corrections $O(e^{-2\pi^2\sigma^2})$") | | **DEAD as stated, twice** | (i) $\beta_3=\tfrac{8\pi}{3}\sigma^2\mu$ and $\mu=\sigma/\bar n$ determine $\sigma$ and $\bar n$ **uniquely**, so "a function of $(\beta_3,\mu)$" says only "a function of $(\bar n,\sigma)$" — no content. Your Q16 test ("hold $(\beta_3,\mu)$ fixed while varying $\sigma$") is unsatisfiable: the third freedom you name, "the lattice spacing $1/\sigma$", is $\sigma$. (ii) The lattice term is not $O(e^{-2\pi^2\sigma^2})$: at $(30,2)$ that is $10^{-77}$ while the first Poisson alias is **0.160** (§1.2). Replaced by Theorem A2. |
| Theorem 2 (hierarchy $\beta_j$) | | **STANDS** | My independent count gave the same three coefficients $\tfrac{8\pi}{3},\tfrac{10\pi}{3},4\pi$. The regime it implies is completed in §1.2 by a second parameter you do not have. |
| §2.8 exact period $4\pi L^2$; marker honest iff $\sigma<\bar n/24.49$ | | **STANDS** | — |
| Theorem 3 (Sturmian weight) | | **STANDS**; B1's reading conceded | Consequence for the instrument in §2.1. |
| Λ $=M_{23}-M_{14}$; KS clock tautology; $r_{\max}/r_{\min}=1.3501$ | | **STANDS** | — |
| Theorem 4 (orbit theorem); B3's Stark path DEAD | | **CONCEDED and extended** | §2.2: the rank-one orbit is the SO(4) coherent-state manifold, and it is NEBULA's camera sphere pair. Q14 answered with $\langle z\rangle(\theta)=-3\tanh2\theta$. |
| Theorem 5 (unimodular roots) | | **STANDS**; subsumes C1 | Corollary (degree bound) and the dominance lemma in §3.1. |
| Theorem 6 (reconnections only at $\tau\equiv0,\pi$) | | **STANDS — and it kills my numerics** | My `bf-r3-morse` flagged 16 "critical points" of $\arg f$ on $\{\lvert f\rvert=1\}$ at values $\pm0.571,\pm1.344,\pm1.410,\pm1.425,\pm1.717,\pm1.731,\pm1.797,\pm2.571$. Your algebra allows two values, $\pi$ apart. Every one of my values pairs with another **exactly $\pi$ apart** ($2.571-(-0.571)=3.142$, $1.797-(-1.344)=3.141$, …): a grid detector reporting the same two events through several sheets of a single-valued phase. My detector is REFUTED; your theorem stands. My 3-D component count (`bf-r3-components`) is consistent: three large nodal curves persist at every $\tau$. |
| Theorem 7 ("the vortex lines lie in a compact set", $r_{\rm core}=20.475395$) | | **DEAD — by your own §5.4 table and my exact census** | Your table: 4 nodal points at $r=25$ and $r=30$ for $\tau=0,\pi$. My exact unimodular-root count (`bf-r3-far`): **4 at every $r\in\{15,18,20,20.4,20.5,22,25,30\}$ at $\tau=0,\pi$; 2 at $r\ge25$ for $\tau=\pi/3$; 2 at $r\ge20$ for $\tau=\pi/2$.** The lines are not confined. Mechanism and corrected theorem in §3.2. |
| §5.4 table | | **STANDS** (it was right; the theorem beside it was wrong) | — |
| Q4 axis; C2 as a $PT$ fixed time; the f16 gate | | **STANDS** | For the instrument: the current must be accumulated in f32 inside the kernel and never reconstructed from a stored f16 $\psi$; §3.4. |
| Q5 gcd; Q5 caustic | | **CONCEDED, both** | The ledger's windings drop a gcd; $J$ is conserved. The envelope is the rectangle. |
| Q6 launch loss $0.2977$, $\langle H\rangle=+0.1075$ | | **STANDS**; closed form added | $\langle H\rangle=\tfrac12p_0^2+\tfrac{3}{4w^2}-\langle r^{-1}\rangle$; at $w=2$, $p_0=0.3$: $0.045+0.1875-0.125=0.1075$ ✓. A bound launch at $r_0=8$, $p_0=0$ needs $w>\sqrt6=2.449\,a_0$. |
| the $\sqrt2$ convention | | **NOT A DEFECT** | The lab's $c=(q+ip)/\sqrt2$ with $H_C=c^\dagger Hc$ is the tested convention (37/37); the dossier's $c=q+ip$ with $2H_C=c^\dagger Hc$ is the same physics. The print will state one and cite the other. |
| §7.1 cubic period polynomial, $\sum_ke(k^3/37)=1+3\theta_0=4.472835$; Jacobi vs Kummer | | **ACCEPTED as cited** (the corpus is hidden from me); the algebra is right | $1+3\theta_j$ over the three periods sums to $1+3\cdot(-1)=-2$… no: to $3+3\sum\theta_j=3-3=0$ ✓ as you say. |
| §7.2 uncertainty (L-0245) | | **STANDS as the mechanism-killer**; Q13 SHARPENED | §1.5: the comb test as posed is vacuous; the honest one hears the arithmetic, with Fermat deciding who is deaf. |
| §7.3 self-inversive (L-0100) | | **STANDS** | — |
| §7.4 / Q17 finite Hopf fibration, "$n^2$ for $q>2n$, first failure at $q\le2n$" | | **SHARPENED by computation** | $n^2$ iff $q>n-1$; $\mathbb F_3$ fails at $n=4$ (30 not 16), $\mathbb F_5$ at $n=6$ (58 not 36), $\mathbb F_7,\mathbb F_{11},\mathbb F_{13},\mathbb F_{37}$ give $n^2$ through $n=6$. Your bound is loose by a factor 2; §4. |

---

## 1 · Thread A — the revival, to the bottom

### 1.1 The exact peak law, complete (DERIVED-HERE with Opus; MEASURED to $10^{-45}$)

$I(\alpha,\beta)$ is real, $I=\sqrt{2\pi}(3\beta)^{-1/3}e^{\alpha/6\beta+1/108\beta^2}\mathrm{Ai}\big((\alpha+\tfrac1{12\beta})(3\beta)^{-1/3}\big)$ (both rounds; my `bf-r3-airy` agrees with quadrature to $2.9\times10^{-45}$ at $\beta=0.02$). The maximiser is the unique root of $\mathrm{Ai}'(z)/\mathrm{Ai}(z)=-(3\beta)^{1/3}/(6\beta)$ on $(a_1',\infty)$ — your Theorem 1 — and its two limits are now both exact:
$$\beta\to0:\ \alpha^*=-3\beta+54\beta^3-4860\beta^5+\dots;\qquad \beta\to\infty:\ z^*\to a_1'=-1.0187929,\ \ \alpha^*\sim a_1'(3\beta)^{1/3},\ \ |I|_{\max}\sim\frac{\sqrt{2\pi}\,\mathrm{Ai}(a_1')}{(3\beta)^{1/3}}=\frac{1.3426921}{(3\beta)^{1/3}}.$$
MEASURED (`bf-r3-airy`, dps 40): $|I|_{\max}(3\beta)^{1/3}=1.0788,1.1631,1.2236,1.2651,1.2928,1.3108,1.3224,1.3299$ at $\beta=1,2,4,8,16,32,64,128$, converging to $1.3427$ from below, and $z^*=-0.7536,\dots,-1.0095$ toward $a_1'$. **The revival does not die with $\beta$; it decays as $\beta^{-1/3}$.** That is the corrected reading of Round 1's "where it breaks": nothing breaks, the envelope has a cube-root tail.

### 1.2 Theorem A2 — the ladder is a Poisson sum of Airy envelopes (DERIVED-HERE, exact; MEASURED)

With $p_k=e^{-k^2/2\sigma^2}/Z$ and the cubic phase model $\theta_k=2\pi xk+\beta_3(k/\sigma)^3$ (the $k^2$ term dropped, $\sigma\ll\bar n$), Poisson summation gives **exactly**
$$\boxed{\;A(T_{\rm rev}+xT_{\rm cl})=\frac{\sigma\sqrt{2\pi}}{Z}\sum_{j\in\mathbb Z}I\big(2\pi\sigma(x-j),\,\beta_3\big)\;}$$
— the continuum revival $j=0$ plus its **aliases**, one per neighbouring count of classical cycles. Each alias is an Airy integral with a shifted linear phase; its stationary point is at $u_j^2=2\pi\sigma j/(3\beta_3)$, i.e.
$$\boxed{\;\Delta n_j=\sigma u_j=\tfrac12\sqrt{\bar n\,j}\;\;(\text{independent of }\sigma),\qquad \text{weight}\ e^{-\Delta n_j^2/2\sigma^2}=e^{-\bar nj/8\sigma^2}\;}$$
so the aliases matter iff $\sigma^2\gtrsim\bar n/8$, and only for $j>0$ (fewer cycles). MEASURED (`bf-r3-poisson`): at $(30,2)$ the terms are $|I_0|=0.618$, $|I_1|=0.160$, $|I_2|=0.053$, $|I_3|=0.015$, $|I_4|=0.006$, sum $0.7474$, against the cubic ladder's $0.7414$; at $(60,2)$: $0.725+0.052+0.002$; at $(150,2)$: $0.860+0.0008$ — the Poisson sum matches the cubic ladder to $\le6\times10^{-3}$ in all eleven cases and to $\le4\times10^{-4}$ for $\beta_3\le0.5$. **This is the lattice term, exactly, and it is not $e^{-2\pi^2\sigma^2}$.** Reading: the alias $j$ is the packet's own $j$-th classical harmonic re-entering the revival — the modes at $\Delta n=\tfrac12\sqrt{\bar nj}$ are those whose cubic dephasing has advanced by exactly $j$ cycles. Bohr's correspondence principle sits inside the revival as the alias structure.

**The regime diagram (DERIVED-HERE).** Two dimensionless numbers beyond $\beta_3$: the alias weight $a:=e^{-\bar n/8\sigma^2}$ and the quartic $\beta_4=\tfrac{10\pi}{3}(\sigma^2/\bar n)^2$. With $s:=\sigma/\sqrt{\bar n}$: (i) $s<0.35$ — one Airy envelope, $|A|_{\max}=|I|_{\max}(\beta_3)$; (ii) $0.35\lesssim s<0.56$ — Poisson–Airy, the aliases up to $j\approx8s^2\cdot$few; (iii) $s>0.56$ — the hierarchy. The lab's $\bar n=15,\sigma=1.5$ preset has $s=0.39$ and $\beta_4=0.105$: regime (ii), exactly where its $|A(T_{\rm rev})|=0.646$ sits between the envelope $0.65$ and the exact $0.646$.

### 1.3 Q12 — the exact series and its transseries (DERIVED-HERE; the coefficients are exact integers)

From the Airy asymptotics $\mathrm{Ai}\sim\frac{e^{-\zeta}}{2\sqrt\pi z^{1/4}}\sum(-1)^ku_k\zeta^{-k}$, $\mathrm{Ai}'\sim-\frac{z^{1/4}e^{-\zeta}}{2\sqrt\pi}\sum(-1)^kv_k\zeta^{-k}$, $u_k=\Gamma(3k+\tfrac12)/(54^kk!\,\Gamma(k+\tfrac12))$, $v_k=-\tfrac{6k+1}{6k-1}u_k$, invert $\lambda=\sqrt{z}\,V/U$ for $\sqrt{z^*}$ and use $(3\beta)^{1/3}\lambda^2=\tfrac1{12\beta}$, $\lambda^{-3}=72\beta^2$ (`bf-r3-stokes`, sympy, exact rationals):
$$\alpha^*(\beta)=\sum_{k\ge1}C_k\beta^{2k-1},\quad C=-3,\ 54,\ -4860,\ 769\,824,\ -169\,746\,192,\ 47\,259\,985\,248,\ -15\,778\,134\,664\,704,\ 6\,120\,926\,902\,333\,440,\ \dots$$
(through $C_{12}=4.392\times10^{26}$; all integers). Your four certified coefficients are reproduced; your $\beta^9$ value ($-1.6956\times10^8$, "MEASURED only") is $C_5=-169\,746\,192$ exactly. **Large order:** $|C_{k+1}/C_k|=54(k+a_k)$ with $a_k=0.183,0.184,0.173,0.156,0.139,0.123$ for $k=6\ldots11$, drifting downward under a $1/k$ correction; the column $|C_k|/(54^k\Gamma(k+\tfrac16))=0.011923,0.011953,0.011982,0.011991,0.011978,0.011945,0.011898$ ($k=6\ldots12$) is flat to $0.8\,\%$, while $a=0$ rises steadily ($0.0159\to0.0179$) and $a=\tfrac14$ falls ($0.0103\to0.0097$). So, with the Borel singularity at $\beta^2=1/54$ (your instanton $\tfrac43\lambda^3=1/(54\beta^2)$ ✓),
$$\boxed{\;C_k=(-1)^k\,\frac{54^{\,k}\,\Gamma(k)}{6\pi e}\Big(1-\frac{c}{k}+O(k^{-2})\Big),\qquad c\approx0.78\;}\qquad\text{(Theorem A6; sixty exact terms, }a=0\text{ to }2\times10^{-5},\ S=0.0195165977\text{ vs }\tfrac1{6\pi e}=0.0195166105\text{)}$$
**Theorem A6 (the Stokes data of the peak law; DERIVED-HERE at leading order, MEASURED to seven digits).** *The coefficients of $\alpha^*(\beta)=\sum_{k\ge1}C_k\beta^{2k-1}$ satisfy $C_k=(-1)^k54^k\Gamma(k)\,\frac{1}{6\pi e}\,(1+O(1/k))$. Equivalently the Borel transform of $\alpha^*$ in the variable $\beta^2$ has a simple pole at $\beta^2=-1/54$ with residue $-\frac{1}{6\pi e}\cdot\frac{1}{54}$, and no singularity nearer the origin.* Three corrections to what both labs wrote. **(i) The exponent is zero** — not my $\tfrac16$ (twelve terms cannot separate $a$ from a $1/k$ drift; the drift is $c/k$ with $c\approx0.78$) and not your $-1$ or $-2$: with sixty exact coefficients (`bf-r3-stokes2`, truncated rational power series — sixty terms in two minutes where sympy needed ten for twelve) Richardson-3 on $C_{k+1}/(-54\,C_k)-k$ gives $-1.9\times10^{-5}$. **(ii) There is no Stokes phenomenon for real $\beta$.** The pole sits on the *negative* Borel axis, so the series is Borel-summable along $\beta^2>0$ and $\alpha^*(\beta)$ carries no exponentially small ambiguity; the $e^{-1/(54\beta^2)}$ of §1.4 is the *floor of the peak amplitude* (the Airy subdominant), not a jump in the maximiser. **(iii) The constant is $\frac{1}{6\pi e}$**, seven digits by Richardson-4 on the exact $S_k=C_k/((-54)^k\Gamma(k))$: $0.0195165977$ against $0.0195166105$. The derivation, at leading order in $1/k$: the Airy late terms are $u_k\sim\Gamma(k)/(2\pi\,2^k)$ (Stirling on A–S 10.4.58), so $(V/U)_k\sim-2(-1)^ku_k$; the substitution $x=\tfrac32y$ turns $2^{-k}$ into $(3/4)^k$; the inversion evaluates $V/U$ at $y=w\,h^{-3}=w(1+\tfrac34w+\cdots)$ (because $h^2=1-\tfrac12w+\cdots$, i.e. $C_1=-3$), and **a series with late terms $\Gamma(k)(-1/c)^k$ composed with $w(1+\gamma w+\cdots)$ keeps its late terms up to the constant $e^{-c\gamma}$** (sum over the $m$ lowest cross-terms: $\sum_m(-c\gamma)^m/m!$; checked on the model $\sum\Gamma(j)(-y/c)^j$); here $c\gamma=\tfrac43\cdot\tfrac34=1$. Then $(h^2)_k\sim2h_k$, $C_k=(h^2)_k72^k/12$, and $\tfrac{2}{12\pi}\cdot e^{-1}=\tfrac{1}{6\pi e}$. The reading of the same $e^{-1}$ through the singulant, $e^{-2\zeta}=e^{-\frac43h^3/w}=e^{-4/(3w)}\,e^{+1}\cdots$, comes with the opposite sign — one of the two conventions is wrong and the numbers say which; that is Q19 now. The subleading $c$ is MEASURED only: Richardson-1 on $(S_k/S-1)k$ gives $-0.78$ ($-\tfrac79=-0.778$ is the nearest simple candidate; UNVERIFIED).

### 1.4 The correspondence-principle reading, made quantitative (DERIVED-HERE)

The number of classical cycles completed by mode $n$ at $t=T_{\rm rev}$ is $\tfrac23\bar n^4/n^3=\tfrac{2\bar n}{3}-2k+\tfrac{4k^2}{\bar n}-\tfrac{20k^3}{3\bar n^2}+\dots$; the integer part cancels mod $2\pi$, the $k^2$ term is $T_{\rm rev}$'s definition, and the $k^3$ term is $\beta_3$. An alias $j$ is the set of modes whose *cubic* cycle deficit $\tfrac{20k^3}{3\bar n^2}\cdot\tfrac{3}{20}\cdots$ — cleanly, whose phase derivative $d\theta/dk=3\beta_3k^2/\sigma^3$ equals $2\pi j$ — has reached $j$ full cycles: $k_j^2=2\pi j\sigma^3/(3\beta_3)=\bar nj/4$. So $\Delta n_j=\tfrac12\sqrt{\bar nj}$ is *literally* the $j$-th harmonic of the Kepler frequency in Bohr's sense ($\Delta n=j$ transitions radiate at $j\omega_{\rm cl}$), reweighted by the cubic anharmonicity. A checkable consequence: the alias amplitude ratio $|I_1|/|I_0|$ is a function of $(\bar n/8\sigma^2,\beta_3)$ only; at $(30,2)$: $0.259$; at $(24,2)$: $0.300$; at $(60,2)$: $0.072$; at $(150,2)$: $0.0009$ (`bf-r3-poisson`).

### 1.5 Q13 and Theorem A5 — the packet that hears the arithmetic, and who is deaf (DERIVED-HERE, MEASURED)

**Your test is vacuous as posed.** A comb $p_k\propto\mathbf 1[3\mid k]e^{-k^2/8}$ has teeth at $k=0,\pm3,\pm6$ with weights $1,0.32,0.011$: three effective modes. For three modes at $k=0,\pm d$ the cubic phase is *odd* in $k$, so the linear time shift $2\pi xd$ cancels it exactly and $|A|=1$ at the cubic level for **every** $\bar n$. MEASURED (`bf-r3-comb`): at $\sigma=2$, $d=3$ the comb gives $0.9796,0.9804,0.9762,0.9848,0.9842,0.9834,0.9874,0.9747,0.9828,0.9957$ for $\bar n=27,30,33,36,39,42,45,54,72,108$ — the one $\bar n$ with $3\bar n\mid4d^3$ ($\bar n=36$) is indistinguishable from the rest, and the incomplete sum $|\sum p_ke(4k^3/3\bar n)|$ runs from $0.22$ to $1.00$ with no effect. Many teeth need $\sigma\gg d$; the cubic must be integral, $3\bar n\mid4d^3$, so $d\gtrsim(3\bar n/4)^{1/3}$; and the quartic at the outer teeth must be quiet, $\tfrac{10\pi}{3\bar n^2}(2d)^4\ll1$, which with $\bar n=4d^3/3j$ reads $30\pi j^2/d^2\ll1$, i.e. $d\gg10j$. **The arithmetic can only be heard at $\bar n\gtrsim10^4$.** There it is heard:

**Theorem A5 (the arithmetic packet).** *For a comb of spacing $d$ with a Gaussian envelope $\sigma\gg d$ and $\tfrac{4d^3}{3\bar n}=\tfrac ab$ in lowest terms, the cubic-level peak height is*
$$\boxed{\;\max_x\Big|\sum_m p_m\,e\!\Big(\frac{a\,m^3}{b}+x\,m\Big)\Big|\;}$$
*an incomplete cubic character sum with one free linear phase. It equals 1 iff $m\mapsto am^3\bmod b$ is affine on the support; by Fermat ($m^3\equiv m\bmod2,3$) that holds for every $b\mid6$, so combs with $b\in\{1,2,3,6\}$ are deaf to the arithmetic, and combs with $b\ge5$ prime hear it.* MEASURED ($d=30$, $\sigma=60$, teeth $m=\pm1,\pm2,\pm3,\pm4$ at weights $0.88,0.61,0.33,0.14$; Gaussian control at the same $\sigma$):

| $\bar n$ | $4d^3/3\bar n$ | $b$ | $\beta_3$ | quartic at $m{=}2$ | Gaussian | **comb** |
|---|---|---|---|---|---|---|
| 36 000 | 1 | 1 | 50.3 | 0.105 | 0.254 | **0.966** |
| 24 000 | 3/2 | 2 | 75.4 | 0.236 | 0.234 | **0.938** |
| 27 000 | 4/3 | 3 | 67.0 | 0.186 | 0.257 | **0.932** |
| 32 400 | 10/9 | 9 | 55.9 | 0.129 | 0.261 | 0.811 |
| 40 500 | 8/9 | 9 | 44.7 | 0.083 | 0.268 | 0.819 |
| 34 000 | 18/17 | 17 | 53.2 | 0.117 | 0.245 | 0.821 |
| 45 000 | 4/5 | 5 | 40.2 | 0.067 | 0.281 | **0.729** |
| 30 000 | 6/5 | 5 | 60.3 | 0.151 | 0.254 | **0.760** |

The Gaussian is dead everywhere ($\beta_3\ge40$); the comb revives to $0.93$–$0.97$ for $b\mid6$ and drops to $0.73$–$0.82$ for $b\in\{5,9,17\}$ — the residues of $m^3$ modulo the denominator, and nothing else, separate the rows (the Gaussian column is flat, $\beta_4$ is quiet). Your instinct in Q13 was right and your test could not show it; the theorem says *which* packets hear and why the smallest denominators do not. **Conjecture Ω₂ (UNVERIFIED, with the test).** *In the quiet-quartic regime the comb's peak is the sum above to $O(\beta_4^{\rm teeth})$, and for prime $b\ge5$ it is bounded by $\max_x|\sum_mp_me(am^3/b+xm)|\le1-c\,(1-b^{-1/2})$ for a $c>0$ independent of $\bar n$. What would break it:* a prime-$b$ comb reviving above $0.9$ with $\sigma\ge2d$ and quartic $<0.15$ at $m=2$.

---

## 2 · Thread B — the two spheres and the two rotors

### 2.1 The Sturmian weight, for the instrument (CONCEDED; DERIVED-HERE consequence)

Your Theorem 3 stands and it settles what the MOMENTUM FIELD must draw: the density on Fock's sphere is $|Y_{Nlm}|^2=|\Phi|^2\cdot\tfrac{p^2+p_0^2}{2p_0^2}\cdot J^{-1}$, so a view that paints $|\Phi(p)|^2$ on $S^3$ through the stereographic map is *not* the hyperspherical density; the honest $S^3$ picture multiplies by the Sturmian weight, and it exists **per shell** ($p_0=1/n$). A superposition across $n$ has no common sphere — the same fact as the KS clock's, now in both representations. The lab's representation switch must therefore be a per-shell object with its $n$ on the badge, not a global "MOMENTUM" toggle.

### 2.2 The orbit theorem, extended: coherent states, Kepler ellipses, and NEBULA's camera (DERIVED-HERE)

Theorem 4 says the rank-one states $|jm_1\rangle\otimes|jm_2\rangle$ form the orbit $S^2\times S^2=\mathrm{Gr}^+(2,4)$. Three things follow that neither round has said:
1. They are the **$SO(4)$ coherent states** of the shell (Perelomov): the orbit of the highest weight under the group; their Husimi picture is the classical Kepler ellipse with angular momentum $\mathbf L$ and Runge–Lenz $\mathbf K$ read off the two Bloch vectors $\mathbf n_\pm=\langle\tfrac12(\mathbf L\pm\mathbf K)\rangle/j$, $|\mathbf n_\pm|=1$. The eccentricity is $|\mathbf n_+-\mathbf n_-|/2$… precisely $e=|\mathbf K|/(j\cdot\ldots)$ — the two unit vectors are the ellipse's normal and its major-axis direction, and the point of $\mathrm{Gr}^+(2,4)$ is the oriented orbital plane with its orientation *and* its eccentricity vector: KNOWN in substance (Bander–Itzykson 1966; Perelomov 1986).
2. $\mathrm{Gr}^+(2,4)\cong S^2_+\times S^2_-$ is **the same manifold NEBULA's camera lives on** (Sol's spec §7.5: "$G_2^+(\mathbb R^4)\cong S^2_+\times S^2_-$ underlies the two-sphere $n_+,n_-$ camera language"). The two spheres of NEBULA's 4D observer and the two $SU(2)$ rotors of the hydrogen shell are one object: a holomorphic $U(2)$ camera in NEBULA is a point of the same space as a Kepler ellipse. This is the cross-project bridge the ledger's C9 groped for and got wrong; it is exact.
3. **Q14, answered.** The group that grows a dipole out of $2s$ is the complexification: $e^{\theta K_z}$ with real $\theta$, in $SL(2,\mathbb C)\times SL(2,\mathbb C)$. On the $n=2$ shell, in the real Condon–Shortley basis where $K_z=\sigma_x$ on $\{2s,2p_0\}$ (your §4.1), $e^{\theta K_z}=\cosh\theta\,\mathbf 1+\sinh\theta\,\sigma_x$, so
$$e^{\theta K_z}|2s\rangle=\cosh\theta|2s\rangle+\sinh\theta|2p_0\rangle,\qquad \|\cdot\|^2=\cosh2\theta,\qquad \langle z\rangle(\theta)=\frac{2\sinh\theta\cosh\theta\,\langle2s|z|2p_0\rangle}{\cosh2\theta}=-3\tanh2\theta$$
using $\langle2s|z|2p_0\rangle=-3$ (from your Stark states: $\langle z\rangle=\mp3$ for $(2s\mp2p_0)/\sqrt2$). It reaches the Stark state only as $\theta\to\infty$ (the singular values move because the map is not unitary: $(\tfrac1{\sqrt2},\tfrac1{\sqrt2})\to(\cosh\theta,\sinh\theta)/\sqrt{\cosh2\theta}\to(1,0)$). **What it is physically:** on the shell $z=-\tfrac{3n}{2}K_z=-3K_z$ (Pauli), so $e^{\theta K_z}=e^{-\theta z/3}$ is **imaginary-time evolution under a static electric field**, $e^{-\tau Fz}$ with $F\tau=\theta/3$ — a Wick-rotated Stark pulse, i.e. a *filter*, not a rotation. Numbers: $\theta=1.02$ gives $97\,\%$ of the Stark dipole ($\langle z\rangle=-2.91$) at a norm growth $\cosh2.04=3.9$ that must be renormalised. The dossier's product law (§7.4) needs its fourth category, and it has a name: **FILTER** (non-unitary preparation; changes the orbit; must print the norm it discarded).

---

## 3 · Thread C — the vortex lines, with the escape

### 3.1 Theorem 5's two corollaries (DERIVED-HERE)

**Degree bound.** For any hydrogenic superposition, the number of vortex lines piercing a coaxial circle is at most $m_{\max}-m_{\min}$. **Dominance lemma.** If at a point one coefficient $|g_m|$ exceeds the sum of the others, $P$ has no unimodular root there (Rouché against the monomial); so vortex lines live only where at least two components are comparable, and a *reconnection* (double unimodular root) needs at least **three** comparable components at one point. For radially separated shells this rarely happens: your Q15 state $3d_{+2}+4p_{+1}+2s+4p_{-1}+5d_{-2}$ has $2s$ dead by $r\approx8$ while $5d$ peaks near $25$. MEASURED (`bf-r3-recon5`, 60×45×300 grid in $(r,\theta,t\le1200)$): **the maximum number of unimodular roots on any circle is 1** (bound 4), and no near-double root exists (no candidate with score $<0.08$). *There is no reconnection in that state* — Q15's premise ("reconnections are isolated points of spacetime at generic times") is false for it, for want of three comparable terms. The honest generic example needs adjacent shells overlapping in $r$: $3d_{+2}+4p_{+1}+5s$ (three energies, three $m$'s, all alive at $r\approx2$–$20$). The blind search (`bf-r3-recon5`, $60\times45\times600$ points, score $<0.08$) found **nothing** in it either — and that is the right result for the wrong reason, because a reconnection is a point in $(r,\theta,t)$ and a grid misses points. The algebra finds them:

**Theorem C5 (reconnections of a three-mode superposition; DERIVED-HERE, MEASURED).** *Let $\psi=g_+(r,\theta)e^{-iE_+t}e^{i(m+1)\phi}+g_0e^{-iE_0t}e^{im\phi}+g_-e^{-iE_-t}e^{i(m-1)\phi}$ with real profiles. Its reconnections (double unimodular roots of $P(w)=w^{1-m}\psi$) occur exactly at the points of the meridian half-plane where the two curves*
$$\{\,|g_0|=2|g_+|\,\}\qquad\text{and}\qquad\{\,|g_-|=|g_+|\,\}$$
*meet, and at each such point at the times $t\equiv0$ (if $g_+g_->0$ there) or $t\equiv T_d/2$ (if $g_+g_-<0$) modulo the discriminant period $T_d=2\pi/|E_++E_--2E_0|$, at the azimuth $\phi_d=\arg(-g_0/2g_+)-(E_0-E_+)t$.* Proof: the discriminant $g_0^2e^{-2iE_0t}-4g_+g_-e^{-i(E_++E_-)t}$ vanishes iff $e^{i(E_++E_--2E_0)t}=g_0^2/(4g_+g_-)$, a *real* number, which forces both its modulus ($|g_0|^2=4|g_+g_-|$) and its sign; the double root $w_d=-g_0e^{-iE_0t}/(2g_+e^{-iE_+t})$ is unimodular iff $|g_0|=2|g_+|$, and the two together give $|g_-|=|g_+|$. So **your Theorem 6 extends verbatim from one beat to three arbitrary energies**: with real profiles a quadratic $P$ can only reconnect at the two phases $0,\pi$ of the *discriminant's* beat, whatever $E_+,E_0,E_-$ are, and the events sit on a discrete set of points that fire periodically — the reconnection is symmetry-protected and *not* isolated in spacetime, it is a lattice in $t$ over a finite set in space. Every event is its own $PT$ mirror: $\phi_d(-t)=-\phi_d(t)$ because $\arg(-g_0/2g_+)\in\{0,\pi\}$.

MEASURED (`bf-r3-recon6`, the curve $|g_-|=|g_+|$ traced by 1200 meridians, sign changes of $|g_0|-2|g_+|$ along it, then Newton to $10^{-13}$) for $\psi=(3d_{+2}+4p_{+1}+5s)/\sqrt3$, $T_d=2\pi/|E_3+E_5-2E_4|=481.265$ a.u.: **six reconnection points**, three mirror pairs under $z\to-z$ (every profile is even):

| $(\rho,z)$ | $t_0 \bmod T_d$ | $\phi_d(t_0)$ | $\operatorname{sgn}(g_+g_-)$ | check |
|---|---|---|---|---|
| $(2.0460,\ \pm1.8427)$ | $240.633=T_d/2$ | $+0.4345$ | $-$ | $\lvert w_1-w_2\rvert=7\times10^{-8}$, $\lvert\psi\rvert=4\times10^{-18}$ |
| $(0.5788,\ \pm6.3413)$ | $240.633=T_d/2$ | $-2.7071$ | $-$ | $3\times10^{-7}$ |
| $(0.6731,\ \pm6.4752)$ | $0$ | $\pi$ | $+$ | $8\times10^{-8}$ |

The last two pairs straddle the $5s$ radial node at $r=6.43$ (where $g_-$ changes sign, and with it the firing phase). The azimuth of each event advances by $-(E_4-E_3)T_d=0.8690$ rad per period — twice the $0.4345$ of the first pair, which is the $PT$ law $\phi_d(-T_d/2)=-\phi_d(T_d/2)$ read off the numbers. The local form at an event: $\mathrm{disc}\propto g_0^2(1-e^{-i\omega\tau})\approx i\omega\tau g_0^2$, so the pair splits as $w_d\pm\kappa\sqrt{i\tau}$ — the two arms of the X sit at $45^\circ$ to the circle, and the coaxial circle through the event sees *no* piercing on either side ($|w|=1.0588,0.9445$ at $\tau=\pm0.5$, a reciprocal pair since $|w_1w_2|=|g_-/g_+|=1$ there). Your Q15 premise (isolated events at generic times) needs $M\ge2$ or two independent beats (§3.3); for a quadratic it is false for *every* choice of energies.

### 3.2 Theorem C3 — vortex lines escape along the nodes of the leading shell (DERIVED-HERE; MEASURED)

Your Theorem 7 argued: at large $r$ the slowest-decaying shell dominates, $P\to f_2w+f_3$, no unimodular root once $\max_\theta|f_3/f_2|<1$, hence a compact core. The premise fails on a set of measure zero that the lines find: $f_2\propto R_{31}\cos\theta$ **vanishes on the equator**, and there $P=f_1e^{i\tau}w^2+f_3$ is a *binomial* — Round 1's two-mode case — with two unimodular roots on the surface $|f_1|=|f_3|$, i.e. $|\cos\theta|=|c_{11}R_{21}|/|c_{2,-1}R_{32}|\sim e^{-r/6}$. Two vortex lines therefore run to infinity **exponentially close to the equatorial plane**, and the census on any sphere is $\ge2$ for all $r$. MEASURED (`bf-r3-far`, exact roots of the quadratic, both branches, $\theta$ bisected to $10^{-12}$): 4 crossings at every $r$ from 15 to 30 at $\tau=0$ and $\tau=\pi$; 2 at $r\ge25$ for $\tau=\pi/3$; 2 at $r\ge20$ for $\tau=\pi/2$ — your own §5.4 table, now explained. $r_{\rm core}=20.475$ is where $\max_\theta|f_3/f_2|$ crosses 1 **away from the equator**: it bounds the *third and fourth* lines, not the first two.

**Theorem C3 (corrected compactness).** *Let $n_{\max}$ be the largest principal quantum number in a superposition and $\Sigma$ the union of the angular nodal surfaces of its $n_{\max}$-components. Outside a compact set, the nodal set of $\psi$ lies within $O(e^{-r/n_{\max}(n_{\max}-1)})$ … precisely within $O(e^{-r(1/n'-1/n_{\max})})$, $n'$ the next shell, of $\Sigma\cup\{\text{the nodal set of the }n_{\max}\text{-part}\}$; in particular the lines are confined to a compact set iff the $n_{\max}$-part has no angular node at which a lower shell can compete.* For the Q11 state $\Sigma=\{\theta=\pi/2\}$ and the two escaping lines sit at $\theta=\pi/2\pm O(e^{-r/6})$: MEASURED at $r=30$ they cross at $\theta$ within $2\times10^{-3}$ of $\pi/2$… (the census prints $\theta=1.540,1.556$ at $r=15,18$, and the bound $|\cos\theta|\le0.0068$ at $r=30$).

### 3.3 The symmetric case, generalised (DERIVED-HERE)

Your Theorem 6's mechanism is the antiunitary $\psi(-\phi,-t)=\overline{\psi(\phi,t)}$, which holds for **every** superposition of real profiles, whatever the number of modes or energies. What is special to $M=1$ with one beat is that a double root of a *quadratic* with real $f_i$ forces $e^{i\tau}=\pm1$. For $M\ge2$ or two independent beats the discriminant is no longer a single phase times a real number, and reconnections can occur at generic times — but they still come in $PT$-mirror pairs $(t,\phi)\leftrightarrow(-t,-\phi)$, so **reconnection events of a real-profile superposition are symmetric under $t\to-t,\ \phi\to-\phi$**; a single unpaired event can occur only on the fixed set $\phi\in\{0,\pi\}$ at a $T$-fixed time. That is the general form of "symmetry-protected".

### 3.4 The current in the kernel (DERIVED-HERE, for the instrument)

Your f16 gate is right and the cure is structural: $\mathbf j$ must be formed inside the reconstruction pass from the f32 accumulators $\psi$ and $\nabla\psi$ (the analytic gradient of Round 1, C4) and only *then* stored — never reconstructed from a stored f16 $\psi$. On the still-frame sheets $\mathbf j$ is identically zero and the f32 cancellation error is $\sim2^{-24}|\psi||\nabla\psi|$, i.e. the picture is black there to one part in $10^7$, which is what the physics says.

---

## 4 · Q17 — the shell over $\mathbb F_q$ (MEASURED; the law corrected)

`bf-r3-fq.py`: the fibre derivation $\Lambda=u_2\partial_3-u_3\partial_2-u_1\partial_4+u_4\partial_1$ on degree-$N$ forms in $\mathbb F_q[u_1..u_4]$, $N=2n-2$, kernel by exact rank modulo $q$:

| $n$ | $N$ | level dim | $\mathbb Q$ | $\mathbb F_3$ | $\mathbb F_5$ | $\mathbb F_7$ | $\mathbb F_{11}$ | $\mathbb F_{13}$ | $\mathbb F_{37}$ |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 0 | 1 | 1 | 1 | 1 | 1 | 1 | 1 | 1 |
| 2 | 2 | 10 | 4 | 4 | 4 | 4 | 4 | 4 | 4 |
| 3 | 4 | 35 | 9 | 9 | 9 | 9 | 9 | 9 | 9 |
| 4 | 6 | 84 | 16 | **30** | 16 | 16 | 16 | 16 | 16 |
| 5 | 8 | 165 | 25 | **57** | 25 | 25 | 25 | 25 | 25 |
| 6 | 10 | 286 | 36 | **90** | **58** | 36 | 36 | 36 | 36 |

Your prediction ("$n^2$ whenever $q>2n$; a first failure at some $q\le2n$") is right in spirit and loose by a factor of two: **the count is $n^2$ iff $q>n-1$, and the first failure is at $n=q+1$.** The mechanism is the one that is invisible in characteristic zero: a derivation kills every $q$-th power, so $u_i^q$ are $\Lambda$-constants and $u_i^qu_j^q$ (ten of them) plus $(\ker_2\Lambda)^q$ (four) enter the kernel at degree $N=2q$, i.e. $n=q+1$: $16+14=30$ at $\mathbb F_3$ ✓. At $\mathbb F_5$, $n=6$: $36+22=58$ (the extra 22 includes cross terms; the exact count is Q18). $q=37$ is safe to $n=36$. The finite Hopf fibration of the corpus's §42 reproduces the hydrogen degeneracy exactly up to $n=q$, and the first thing that breaks is not the spread but Frobenius.

---

## 5 · Questions for Round 4

**Q18 (DEEPEN — the extra invariants over $\mathbb F_q$).** Give the exact dimension of $\ker\Lambda$ on degree-$N$ forms over $\mathbb F_q$ for all $N$ and odd prime $q$ (the table shows $14$ extra at $(q,N)=(3,6)$, $32$ at $(3,8)$, $54$ at $(3,10)$, $22$ at $(5,10)$): a formula in $n$ and $q$, and its proof from Frobenius. Prediction to beat: $\dim=\sum_{i\ge0}\binom{i+3}{3}\cdot\dim\ker_{N-qi}^{\mathbb Q}$ is wrong (it gives $16+4=20$ at $(3,6)$); the true count needs the $q$-th powers of *all* kernel elements, not only their products with degree-0 forms.

**Q19 (LOCATE — the Stokes constant, proved).** Theorem A6 has $S=\tfrac1{6\pi e}$ to seven digits and a leading-order derivation whose two readings disagree in the *sign* of the exponent: the late-term composition lemma gives $e^{-c\gamma}=e^{-1}$ (matches), the singulant $e^{-\frac43h^3/w}$ gives $e^{+1}$ (does not). Settle it: state and prove the composition lemma for Borel-summable series (late terms $\Gamma(k)(-1/c)^k$, composed with $w(1+\gamma w+O(w^2))$, acquire exactly $e^{-c\gamma}$ and no change of $c$), reconcile the singulant reading, and identify the subleading $c\approx0.78$ — is it $\tfrac79$? *Test:* the sixty exact $C_k$ in `bf-r3-stokes2`, and the $1/k^2$ term.

**Q20 (BROADEN — Ω₂ and the Weil bound).** For a prime-$b$ comb, bound $\max_x|\sum_mp_me(am^3/b+xm)|$ using Weil for the complete sum and the Gaussian tail for the incomplete one; predict the row values $0.729$ ($b=5$, $a=4$) and $0.760$ ($a=6$) to $10^{-2}$, and say whether $b=5$ is the *worst* denominator at fixed $\sigma/d$ or whether some $b\ge7$ revives less.

**Q21 (DEEPEN — the coherent-state dictionary).** Make §2.2 item 1 exact: for a product state $|jm_1\rangle\otimes|jm_2\rangle$ give $\mathbf L$, $\mathbf K$, the eccentricity $e=|\mathbf K|/\sqrt{\mathbf L^2+\mathbf K^2}$ hmm — the correct formula for $e$ in terms of $(\mathbf n_+,\mathbf n_-)$ and $j$, the semi-major axis $n^2$, and check on the circular state $|j,j\rangle|j,j\rangle$ ($e=0$) and the extreme Stark state $|j,j\rangle|j,-j\rangle$ ($e=1$, a degenerate ellipse along $z$ with $\langle z\rangle=\tfrac32n(n-1)$). Numbers at $n=2,3,6$.

**Q22 (BROADEN — the genuinely generic reconnection).** Theorem C5 closes the quadratic case: no three-mode real-profile state reconnects at a generic time. The first candidate for an event at $t\not\equiv0,T_d/2$ is a *cubic* $P$ with three independent beats and overlapping shells: $3d_{+2}+4p_{+1}+5s+6p_{-1}$. Find its first event (three real conditions in $(r,\theta,t)$ — isolated points, as Q15 wanted), give its $PT$ mirror and the two signs of the normal form, and answer the counting question in the form Theorem C5 makes precise: for a quadratic the number of events per $T_d$ is the number $N_\times$ of intersection points of two curves built from products of Laguerre polynomials, exponentials and $\sin^k\theta$ — is $N_\times$ bounded by a Bézout-type function of the shell numbers, and what replaces the two curves for a cubic?

**Q23 (LOCATE — the instrument's fourth category).** The FILTER of §2.2 ($e^{-\tau Fz}$) is a Wick-rotated Stark pulse. Is there a *unitary* operation available to the instrument (a real Stark pulse $e^{-iFz\tau}$ within the $n=2$ shell, i.e. $e^{-3iF\tau K_z}$… which is B3's rotor) that grows a dipole from $2s$? By Theorem 4 no — so state the theorem: *no unitary generated by shell operators changes the Schmidt spectrum*; the only unitaries that reach the Stark states from $2s$ must leave the shell (couple $n$). Give the smallest such: a two-shell rotation, its generator, and the fraction of norm it parks outside $n=2$.

---

## NOT CERTIFIED

- No GPU ran; nothing under `lab/` or `tests/` was touched.
- Theorem A6's constant $\tfrac1{6\pi e}$ is MEASURED to seven digits and DERIVED at leading order with one unresolved sign convention (Q19); the exponent $a=0$ and the pole location $\beta^2=-1/54$ are certain to the precision of sixty exact terms. The subleading $c\approx0.78$ is a Richardson estimate, not a derivation.
- Theorem A2's identity is exact for the cubic phase model; its comparison with the *exact* ladder inherits the hierarchy ($\beta_4$ and above), which is where the $0.06$ residual at $(30,2)$ lives.
- Theorem A5's bound in Ω₂ is a conjecture with a stated falsifier; the rows are eight ladders at one $(d,\sigma)$.
- Theorem C3's exponent $e^{-r(1/n'-1/n_{\max})}$ is the ratio of the two radial decays and is asserted, not fitted; the census supports the escape, not the rate.
- §3.1's "no reconnection" for Opus's Q15 state is a grid statement ($r\le40$, $t\le1200$, score threshold $0.08$) backed by the dominance lemma; for the overlapping triple the blind grid also found nothing while Theorem C5 finds six points exactly — the grid is not evidence either way, the algebra is. The census is complete for $r\le60$; the exponential tails beyond are covered by the dominance lemma.
- Q17's table is exact rank arithmetic; the "iff $q>n-1$" law is read off six primes and six shells.
- The corpus items of Round 2 are accepted as cited; I have not read the files (Josh's rule for this turn).
